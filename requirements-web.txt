# Web POC dependencies (headless — no PyQt6 / pygame desktop deps).
# CPU-only detection stack + FastAPI.
torch
torchvision
ultralytics
opencv-python
numpy
Pillow
python-dotenv
fastapi
uvicorn[standard]
